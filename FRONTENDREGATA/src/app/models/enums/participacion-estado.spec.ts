import { ParticipacionEstado } from './participacion-estado';

describe('ParticipacionEstado', () => {
  it('should create an instance', () => {
    expect(new ParticipacionEstado()).toBeTruthy();
  });
});
